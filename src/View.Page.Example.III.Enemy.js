import * as THREE from 'three'

import React from './utils.react'
import ReactPlugin from './utils.react.plugin'

import Enemy from './View.Page.Example.III.Enemy.Component'

const App = () => {
  // const context = React.useContext()

  // const Object0x000 = React.useMemo(() => {
  //   const group = new THREE.Group()

  //   group.position.x = 0
  //   group.position.y = 0
  //   group.position.z = 0

  //   return group
  // }, [])

  // context.json.enemy.forEach(i => Enemy({ ...i, target: Object0x000 }))

  // ReactPlugin.useObject({ target: context.scene, object: Object0x000 })
}

export default React.component(App)